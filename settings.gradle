pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        maven {
            url 'https://repo.spring.io/milestone'
        }
    }
}

rootProject.name = 'hello-spring-boot-gradle'
